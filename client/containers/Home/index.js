import React from "react";
import StoryList from "../../components/StoryList";

const Home = () => (
    <div className="row">
        <main className="card">
            <StoryList/>
        </main>
    </div>
);

export default Home;
